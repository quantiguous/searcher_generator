require "spec_helper"

describe SearcherGenerator do
  it "has a version number" do
    expect(SearcherGenerator::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
